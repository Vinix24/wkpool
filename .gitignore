# Secrets — never commit
.env

# Runtime data (downloaded / generated, not redistributable)
data/
output/
models/

# Your private plugins, weights and predictions — your edge stays yours
plugins_user/
weights.local.yaml
PREDICTIONS.local.md

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.pytest_cache/
dist/
build/
