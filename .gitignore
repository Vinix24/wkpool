# Secrets — never commit
.env

# Runtime data (downloaded / generated, not redistributable)
data/
output/
models/

# Your private plugins and weights — your edge stays yours
plugins_user/
weights.local.yaml

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.pytest_cache/
dist/
build/
